#!/usr/bin/guile -s
!#

#!

Our encode-symbol procedure is O(N^2) in the case of the previous exercise, but on average it will be better
than this. On average we will have a time complexity of O(n log(n)).

!#
